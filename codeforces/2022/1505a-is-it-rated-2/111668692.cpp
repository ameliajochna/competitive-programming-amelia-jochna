#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solution() {
  string s;
  while (getline(cin, s)) {
    cout << "NO\n";
    fflush(stdout);
  }
}

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie();
  cout.tie();
  int t = 1;
  while (t--) solution();
}
